{
  "config": {
    "step": {
      "user": {
        "title": "Add a MEATER probe",
        "description": "Take the probe out of its charger and keep it within range of a Bluetooth adapter or proxy, then pick it below. If your probe isn't listed, wait a few seconds and try again - some probes only broadcast their name intermittently, so it may appear as a plain MAC address.",
        "data": {
          "address": "MEATER probe"
        }
      },
      "bluetooth_confirm": {
        "description": "Add **{name}** ({address}) - {manufacturer} {model}?\n\nThe probe will be read locally over Bluetooth. No cloud account required.",
        "title": "Confirm MEATER probe"
      }
    },
    "abort": {
      "already_configured": "This probe is already configured.",
      "dock": "That's the MEATER charger/dock, not the probe. The dock can't be read directly. Take the probe out of the charger and it will be discovered on its own.",
      "no_devices_found": "No Bluetooth devices found. Make sure the probe is out of its charger, turned on and within range of a Bluetooth adapter or proxy, then try again."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "MEATER BLE options",
        "data": {
          "keepalive_interval": "Keepalive read interval (seconds)",
          "force_keepalive_interval": "Force the shorter keepalive interval for MEATER / MEATER+",
          "reconnect_timeout": "Reconnect timeout for zero-length packets (seconds)"
        },
        "data_description": {
          "keepalive_interval": "How often a MEATER 2 Plus / Pro is actively read to keep its Bluetooth link alive. A lower value can hold a probe that keeps dropping after a few minutes, at the cost of more Bluetooth traffic. Default 8.",
          "force_keepalive_interval": "Apply the shorter keepalive interval above to the original MEATER / MEATER+ too, instead of using the default 20-second poll.",
          "reconnect_timeout": "How long a reachable probe may keep returning only zero-length packets before the integration tears the Bluetooth link down and reconnects. Default 120."
        }
      }
    }
  },
  "entity": {
    "sensor": {
      "tip_temp": {
        "name": "Tip Temperature"
      },
      "ambient_temp": {
        "name": "Ambient Temperature"
      },
      "battery": {
        "name": "Battery"
      },
      "cook_state": {
        "name": "Cook Status",
        "state": {
          "idle": "Idle",
          "cooking": "Cooking",
          "approaching_target": "Approaching Target",
          "resting": "Resting"
        }
      },
      "signal_strength": {
        "name": "Signal Strength"
      }
    }
  }
}
